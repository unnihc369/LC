# JavaScript Solutions

## Questions

Click a question to jump to its detailed solution.

| # | Question |
|---|---|
| 1 | [First non-repeating character in a string](#1-first-non-repeating-character) |
| 2 | [Rotate array right by `k` without extra array](#2-rotate-array-right-by-k-no-extra-array) |
| 3 | [Longest substring without repeating characters](#3-longest-substring-without-repeating-characters) |
| 4 | [Move all zeroes to end (stable order)](#4-move-all-zeroes-to-end-stable-order) |
| 5 | [Check if two strings are anagrams using frequency](#5-check-anagram-using-frequency) |
| 6 | [Index of second highest number](#6-index-of-second-highest-number) |
| 7 | [Remove duplicate characters from a string (keep first order)](#7-remove-duplicate-characters-keep-first-order) |
| 8 | [Maximum sum of any contiguous subarray](#8-maximum-sum-of-any-contiguous-subarray) |
| 9 | [Reverse word order without using split()](#9-reverse-word-order-without-using-split) |
| 10 | [All unique pairs that sum to target](#10-all-unique-pairs-that-sum-to-target) |
| 11 | [Sentence palindrome ignoring spaces/punctuation](#11-sentence-palindrome-ignoring-spacespunctuation) |
| 12 | [Check if array contains duplicates](#12-check-if-array-contains-duplicates) |
| 13 | [Replace spaces with %20](#13-replace-spaces-with-20) |
| 14 | [Find majority element (> n/2)](#14-find-majority-element--n2) |
| 15 | [Character frequency map](#15-character-frequency-map) |
| 16 | [Remove duplicate values from array](#16-remove-duplicate-values-from-array) |
| 17 | [Longest palindromic substring](#17-longest-palindromic-substring) |
| 18 | [Find missing number (0..n)](#18-find-missing-number-0n) |
| 19 | [Equal strings after backspace processing](#19-equal-strings-after-backspace-processing) |
| 20 | [Merge two sorted arrays](#20-merge-two-sorted-arrays) |
| 21 | [Reverse each word, keep word order](#21-reverse-each-word-keep-word-order) |
| 22 | [Smallest value in rotated sorted array](#22-smallest-value-in-rotated-sorted-array) |
| 23 | [All permutations of a string](#23-all-permutations-of-a-string) |
| 24 | [Product except self](#24-product-except-self) |
| 25 | [Check if two strings follow same pattern](#25-check-if-two-strings-follow-same-pattern) |
| 26 | [Partition array: evens first, odds last](#26-partition-array-evens-first-odds-last) |
| 27 | [Validate balanced brackets](#27-validate-balanced-brackets) |
| 28 | [Return all subsets of an array](#28-return-all-subsets-of-an-array) |
| 29 | [Remove all vowels from a string](#29-remove-all-vowels-from-a-string) |
| 30 | [Count subarrays with sum = target](#30-count-subarrays-with-sum--target) |
| 31 | [Longest word in a string](#31-longest-word-in-a-string) |
| 32 | [All unique triplets summing to zero](#32-all-unique-triplets-summing-to-zero) |
| 33 | [Compress string with run counts](#33-compress-string-with-run-counts) |
| 34 | [Reverse array without reverse()](#34-reverse-array-without-reverse) |
| 35 | [Remove chars of second string from first](#35-remove-chars-of-second-string-from-first) |
| 36 | [Longest increasing subsequence](#36-longest-increasing-subsequence) |
| 37 | [Check if one string is rotation of another](#37-check-if-one-string-is-rotation-of-another) |
| 38 | [Count pairs differing by exactly k](#38-count-pairs-differing-by-exactly-k) |
| 39 | [Roman numeral to integer](#39-roman-numeral-to-integer) |
| 40 | [First non-repeating element in array](#40-first-non-repeating-element-in-array) |
| 41 | [Pangram test](#41-pangram-test) |
| 42 | [Merge overlapping intervals](#42-merge-overlapping-intervals) |
| 43 | [All palindromic substrings](#43-all-palindromic-substrings) |
| 44 | [Longest consecutive sequence in array](#44-longest-consecutive-sequence-in-array) |
| 45 | [Remove repeated sequential characters](#45-remove-repeated-sequential-characters) |
| 46 | [Find a peak element](#46-find-a-peak-element) |
| 47 | [Check subsequence](#47-check-subsequence) |
| 48 | [Sort array of 0s, 1s, and 2s](#48-sort-array-of-0s-1s-and-2s) |
| 49 | [Convert string chars to ASCII list](#49-convert-string-chars-to-ascii-list) |
| 50 | [Count subarrays with equal 0s and 1s](#50-count-subarrays-with-equal-0s-and-1s) |
| 51 | [Reverse only vowels](#51-reverse-only-vowels) |
| 52 | [Smallest subarray sum at least target](#52-smallest-subarray-sum-at-least-target) |
| 53 | [Convert sentence to title case words](#53-convert-sentence-to-title-case-words) |
| 54 | [Elements appearing more than n/3 times](#54-elements-appearing-more-than-n3-times) |
| 55 | [Check if all chars are unique](#55-check-if-all-chars-are-unique) |
| 56 | [Trapping rain water](#56-trapping-rain-water) |
| 57 | [Reorganize string (no equal adjacent)](#57-reorganize-string-no-equal-adjacent) |
| 58 | [kth smallest value in array](#58-kth-smallest-value-in-array) |
| 59 | [Replace first occurrence of substring](#59-replace-first-occurrence-of-substring) |
| 60 | [Check rotated sorted array](#60-check-rotated-sorted-array) |
| 61 | [Count substrings starting and ending same](#61-count-substrings-starting-and-ending-same) |
| 62 | [Return all duplicate numbers](#62-return-all-duplicate-numbers) |
| 63 | [Validate simplified email](#63-validate-simplified-email) |
| 64 | [Count inversions in array](#64-count-inversions-in-array) |
| 65 | [Capitalize first letter of each sentence](#65-capitalize-first-letter-of-each-sentence) |
| 66 | [Container with most water](#66-container-with-most-water) |
| 67 | [Check if can permute to palindrome](#67-check-if-can-permute-to-palindrome) |
| 68 | [Minimum jumps to reach end](#68-minimum-jumps-to-reach-end) |
| 69 | [Remove consecutive duplicate words](#69-remove-consecutive-duplicate-words) |
| 70 | [Running sums of array](#70-running-sums-of-array) |
| 71 | [Validate password strength rules](#71-validate-password-strength-rules) |
| 72 | [Group anagrams from string array](#72-group-anagrams-from-string-array) |
| 73 | [Split string into chunks of k](#73-split-string-into-chunks-of-k) |
| 74 | [Next greater element for each value](#74-next-greater-element-for-each-value) |
| 75 | [Lexicographically smallest after removing k chars](#75-lexicographically-smallest-after-removing-k-chars) |
| 76 | [Rotate array left by k](#76-rotate-array-left-by-k) |
| 77 | [Phone keypad letter combinations](#77-phone-keypad-letter-combinations) |
| 78 | [Minimum missing positive integer](#78-minimum-missing-positive-integer) |
| 79 | [Keep only English letters](#79-keep-only-english-letters) |
| 80 | [All contiguous subarrays](#80-all-contiguous-subarrays) |
| 81 | [Common words in two sentences](#81-common-words-in-two-sentences) |
| 82 | [Alternate positive and negative numbers](#82-alternate-positive-and-negative-numbers) |
| 83 | [Longest valid parentheses substring](#83-longest-valid-parentheses-substring) |
| 84 | [Count how many times each element appears](#84-count-how-many-times-each-element-appears) |
| 85 | [Regex match with dot and star](#85-regex-match-with-dot-and-star) |
| 86 | [Intersection of two arrays](#86-intersection-of-two-arrays) |
| 87 | [Remove chars that appear more than once](#87-remove-chars-that-appear-more-than-once) |
| 88 | [Sorted array: remove duplicates length](#88-sorted-array-remove-duplicates-length) |
| 89 | [Count digits in a string](#89-count-digits-in-a-string) |
| 90 | [Pair with largest product](#90-pair-with-largest-product) |
| 91 | [Reverse only alphabets (mirror)](#91-reverse-only-alphabets-mirror) |
| 92 | [Subarray with sum zero exists](#92-subarray-with-sum-zero-exists) |
| 93 | [Mask string (last 4 visible)](#93-mask-string-last-4-visible) |
| 94 | [Median of two sorted arrays](#94-median-of-two-sorted-arrays) |
| 95 | [Maximum rearrangement (lex or numeric string)](#95-maximum-rearrangement-lex-or-numeric-string) |
| 96 | [Sort by frequency (high first)](#96-sort-by-frequency-high-first) |
| 97 | [Trim without built-in trim](#97-trim-without-built-in-trim) |
| 98 | [Longest mountain subarray](#98-longest-mountain-subarray) |
| 99 | [Repeating substring pattern](#99-repeating-substring-pattern) |
| 100 | [Sort strings by length](#100-sort-strings-by-length) |

---

## 1) First non-repeating character

### Approach A: Frequency map
```js
function firstNonRepeatingCharMap(s) {
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  for (const ch of s) if (freq.get(ch) === 1) return ch;
  return null;
}
```

### Approach B: `indexOf` + `lastIndexOf`
```js
function firstNonRepeatingCharIndex(s) {
  for (const ch of s) if (s.indexOf(ch) === s.lastIndexOf(ch)) return ch;
  return null;
}
```

---

## 2) Rotate array right by k (no extra array)

### Approach A: Reverse in-place (optimal)
```js
function rotateRightReverse(nums, k) {
  const n = nums.length;
  if (n === 0) return nums;
  const step = ((k % n) + n) % n;
  reverse(nums, 0, n - 1);
  reverse(nums, 0, step - 1);
  reverse(nums, step, n - 1);
  return nums;
}

function reverse(arr, l, r) {
  while (l < r) {
    const t = arr[l];
    arr[l++] = arr[r];
    arr[r--] = t;
  }
}
```

### Approach B: Repeated 1-step rotation (in-place, slower)
```js
function rotateRightRepeated(nums, k) {
  const n = nums.length;
  if (n === 0) return nums;
  const step = ((k % n) + n) % n;
  for (let i = 0; i < step; i++) {
    const last = nums[n - 1];
    for (let j = n - 1; j > 0; j--) nums[j] = nums[j - 1];
    nums[0] = last;
  }
  return nums;
}
```

---

## 3) Longest substring without repeating characters

### Approach A: Sliding window + Set
```js
function longestSubstringNoRepeatSet(s) {
  const seen = new Set();
  let left = 0;
  let best = 0;
  for (let right = 0; right < s.length; right++) {
    while (seen.has(s[right])) seen.delete(s[left++]);
    seen.add(s[right]);
    best = Math.max(best, right - left + 1);
  }
  return best;
}
```

### Approach B: Sliding window + last index map
```js
function longestSubstringNoRepeatMap(s) {
  const last = new Map();
  let left = 0;
  let best = 0;
  for (let right = 0; right < s.length; right++) {
    const ch = s[right];
    if (last.has(ch) && last.get(ch) >= left) left = last.get(ch) + 1;
    last.set(ch, right);
    best = Math.max(best, right - left + 1);
  }
  return best;
}
```

---

## 4) Move all zeroes to end (stable order)

### Approach A: Swap with write pointer
```js
function moveZeroesSwap(nums) {
  let write = 0;
  for (let read = 0; read < nums.length; read++) {
    if (nums[read] !== 0) {
      const t = nums[write];
      nums[write] = nums[read];
      nums[read] = t;
      write++;
    }
  }
  return nums;
}
```

### Approach B: Compact then fill zeros
```js
function moveZeroesFill(nums) {
  let write = 0;
  for (let i = 0; i < nums.length; i++) if (nums[i] !== 0) nums[write++] = nums[i];
  while (write < nums.length) nums[write++] = 0;
  return nums;
}
```

---

## 5) Check anagram using frequency

### Approach A: Hash map frequency
```js
function isAnagramMap(s, t) {
  if (s.length !== t.length) return false;
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  for (const ch of t) {
    if (!freq.has(ch)) return false;
    const next = freq.get(ch) - 1;
    if (next === 0) freq.delete(ch);
    else freq.set(ch, next);
  }
  return freq.size === 0;
}
```

### Approach B: 26-length array (lowercase only)
```js
function isAnagramArrayLowercase(s, t) {
  if (s.length !== t.length) return false;
  const count = Array(26).fill(0);
  for (let i = 0; i < s.length; i++) {
    count[s.charCodeAt(i) - 97]++;
    count[t.charCodeAt(i) - 97]--;
  }
  for (const x of count) if (x !== 0) return false;
  return true;
}
```

---

## 6) Index of second highest number

### Approach A: Distinct second highest
```js
function indexOfSecondHighestDistinct(nums) {
  if (nums.length < 2) return -1;
  let maxVal = -Infinity, maxIdx = -1;
  let secondVal = -Infinity, secondIdx = -1;
  for (let i = 0; i < nums.length; i++) {
    const x = nums[i];
    if (x > maxVal) {
      secondVal = maxVal; secondIdx = maxIdx;
      maxVal = x; maxIdx = i;
    } else if (x < maxVal && x > secondVal) {
      secondVal = x; secondIdx = i;
    }
  }
  return secondIdx;
}
```

### Approach B: Allow tie on max (second occurrence)
```js
function indexOfSecondHighestAllowTie(nums) {
  if (nums.length < 2) return -1;
  let maxVal = -Infinity;
  for (const x of nums) if (x > maxVal) maxVal = x;
  let seen = 0;
  for (let i = 0; i < nums.length; i++) {
    if (nums[i] === maxVal) {
      seen++;
      if (seen === 2) return i;
    }
  }
  return indexOfSecondHighestDistinct(nums);
}
```

---

## 7) Remove duplicate characters (keep first order)

```js
function removeDuplicateCharsKeepOrder(s) {
  const seen = new Set();
  let out = "";
  for (const ch of s) {
    if (!seen.has(ch)) {
      seen.add(ch);
      out += ch;
    }
  }
  return out;
}
```

---

## 8) Maximum sum of any contiguous subarray

### Approach A: Kadane's algorithm
```js
function maxSubarraySum(nums) {
  let best = -Infinity;
  let cur = 0;
  for (const x of nums) {
    cur = Math.max(x, cur + x);
    best = Math.max(best, cur);
  }
  return best;
}
```

---

## 9) Reverse word order without using split()

```js
function reverseWordOrderNoSplit(s) {
  const words = [];
  let i = 0;
  while (i < s.length) {
    while (i < s.length && s[i] === " ") i++;
    if (i >= s.length) break;
    let start = i;
    while (i < s.length && s[i] !== " ") i++;
    words.push(s.slice(start, i));
  }
  let out = "";
  for (let j = words.length - 1; j >= 0; j--) {
    out += words[j];
    if (j > 0) out += " ";
  }
  return out;
}
```

---

## 10) All unique pairs that sum to target

```js
function uniquePairsWithTarget(nums, target) {
  const seen = new Set();
  const pairs = new Set();
  const out = [];
  for (const x of nums) {
    const y = target - x;
    if (seen.has(y)) {
      const a = Math.min(x, y);
      const b = Math.max(x, y);
      const key = `${a},${b}`;
      if (!pairs.has(key)) {
        pairs.add(key);
        out.push([a, b]);
      }
    }
    seen.add(x);
  }
  return out;
}
```

---

## 11) Sentence palindrome ignoring spaces/punctuation

```js
function isSentencePalindrome(s) {
  let l = 0, r = s.length - 1;
  while (l < r) {
    while (l < r && !isAlphaNum(s[l])) l++;
    while (l < r && !isAlphaNum(s[r])) r--;
    if (s[l].toLowerCase() !== s[r].toLowerCase()) return false;
    l++;
    r--;
  }
  return true;
}

function isAlphaNum(ch) {
  const code = ch.charCodeAt(0);
  return (
    (code >= 48 && code <= 57) ||
    (code >= 65 && code <= 90) ||
    (code >= 97 && code <= 122)
  );
}
```

---

## 12) Check if array contains duplicates

```js
function containsDuplicate(nums) {
  const seen = new Set();
  for (const x of nums) {
    if (seen.has(x)) return true;
    seen.add(x);
  }
  return false;
}
```

---

## 13) Replace spaces with %20

```js
function urlEncodeSpaces(s) {
  let out = "";
  for (const ch of s) out += ch === " " ? "%20" : ch;
  return out;
}
```

---

## 14) Find majority element (> n/2)

### Approach A: Boyer-Moore voting
```js
function majorityElement(nums) {
  let candidate = null;
  let count = 0;
  for (const x of nums) {
    if (count === 0) candidate = x;
    count += x === candidate ? 1 : -1;
  }
  return candidate;
}
```

---

## 15) Character frequency map

```js
function charFrequencyMap(s) {
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  return freq;
}
```

---

## 16) Remove duplicate values from array

```js
function uniqueArray(nums) {
  return [...new Set(nums)];
}
```

---

## 17) Longest palindromic substring

```js
function longestPalindromicSubstring(s) {
  if (s.length < 2) return s;
  let bestL = 0, bestR = 0;
  for (let i = 0; i < s.length; i++) {
    expand(i, i);
    expand(i, i + 1);
  }
  return s.slice(bestL, bestR + 1);

  function expand(l, r) {
    while (l >= 0 && r < s.length && s[l] === s[r]) {
      if (r - l > bestR - bestL) {
        bestL = l;
        bestR = r;
      }
      l--;
      r++;
    }
  }
}
```

---

## 18) Find missing number (0..n)

```js
function missingNumber(nums) {
  const n = nums.length;
  let expected = (n * (n + 1)) / 2;
  let actual = 0;
  for (const x of nums) actual += x;
  return expected - actual;
}
```

---

## 19) Equal strings after backspace processing

```js
function backspaceCompare(s, t) {
  return build(s) === build(t);
}

function build(str) {
  const st = [];
  for (const ch of str) {
    if (ch === "#") st.pop();
    else st.push(ch);
  }
  return st.join("");
}
```

---

## 20) Merge two sorted arrays

```js
function mergeSortedArrays(a, b) {
  const out = [];
  let i = 0, j = 0;
  while (i < a.length && j < b.length) {
    if (a[i] <= b[j]) out.push(a[i++]);
    else out.push(b[j++]);
  }
  while (i < a.length) out.push(a[i++]);
  while (j < b.length) out.push(b[j++]);
  return out;
}
```

---

## 21) Reverse each word, keep word order

```js
function reverseEachWordKeepOrder(s) {
  let out = "";
  let i = 0;
  while (i < s.length) {
    if (s[i] === " ") {
      out += " ";
      i++;
      continue;
    }
    let start = i;
    while (i < s.length && s[i] !== " ") i++;
    for (let j = i - 1; j >= start; j--) out += s[j];
  }
  return out;
}
```

---

## 22) Smallest value in rotated sorted array

```js
function findMinRotated(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    const m = Math.floor((l + r) / 2);
    if (nums[m] > nums[r]) l = m + 1;
    else r = m;
  }
  return nums[l];
}
```

---

## 23) All permutations of a string

```js
function permutations(str) {
  const chars = str.split("");
  const out = [];
  backtrack(0);
  return out;

  function backtrack(i) {
    if (i === chars.length) {
      out.push(chars.join(""));
      return;
    }
    const used = new Set();
    for (let j = i; j < chars.length; j++) {
      if (used.has(chars[j])) continue;
      used.add(chars[j]);
      [chars[i], chars[j]] = [chars[j], chars[i]];
      backtrack(i + 1);
      [chars[i], chars[j]] = [chars[j], chars[i]];
    }
  }
}
```

---

## 24) Product except self

```js
function productExceptSelf(nums) {
  const n = nums.length;
  const out = Array(n).fill(1);
  for (let i = 1; i < n; i++) out[i] = out[i - 1] * nums[i - 1];
  let suffix = 1;
  for (let i = n - 1; i >= 0; i--) {
    out[i] *= suffix;
    suffix *= nums[i];
  }
  return out;
}
```

---

## 25) Check if two strings follow same pattern

```js
function samePattern(a, b) {
  if (a.length !== b.length) return false;
  const ab = new Map();
  const ba = new Map();
  for (let i = 0; i < a.length; i++) {
    const x = a[i], y = b[i];
    if ((ab.has(x) && ab.get(x) !== y) || (ba.has(y) && ba.get(y) !== x)) return false;
    ab.set(x, y);
    ba.set(y, x);
  }
  return true;
}
```

---

## 26) Partition array: evens first, odds last

```js
function partitionEvenOdd(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    while (l < r && nums[l] % 2 === 0) l++;
    while (l < r && nums[r] % 2 !== 0) r--;
    if (l < r) [nums[l], nums[r]] = [nums[r], nums[l]];
  }
  return nums;
}
```

---

## 27) Validate balanced brackets

```js
function isBalancedBrackets(s) {
  const st = [];
  const closeToOpen = new Map([ [")", "("], ["]", "["], ["}", "{"] ]);
  for (const ch of s) {
    if (closeToOpen.has(ch)) {
      if (st.pop() !== closeToOpen.get(ch)) return false;
    } else if (ch === "(" || ch === "[" || ch === "{") {
      st.push(ch);
    }
  }
  return st.length === 0;
}
```

---

## 28) Return all subsets of an array

```js
function subsets(nums) {
  const out = [];
  const cur = [];
  backtrack(0);
  return out;

  function backtrack(i) {
    if (i === nums.length) {
      out.push([...cur]);
      return;
    }
    backtrack(i + 1);
    cur.push(nums[i]);
    backtrack(i + 1);
    cur.pop();
  }
}
```

---

## 29) Remove all vowels from a string

```js
function removeVowels(s) {
  const vowels = new Set(["a", "e", "i", "o", "u"]);
  let out = "";
  for (const ch of s) if (!vowels.has(ch.toLowerCase())) out += ch;
  return out;
}
```

---

## 30) Count subarrays with sum = target

```js
function countSubarraysSumK(nums, k) {
  const prefixCount = new Map([[0, 1]]);
  let sum = 0;
  let ans = 0;
  for (const x of nums) {
    sum += x;
    ans += prefixCount.get(sum - k) || 0;
    prefixCount.set(sum, (prefixCount.get(sum) || 0) + 1);
  }
  return ans;
}
```

---

## 31) Longest word in a string

```js
function longestWord(s) {
  let best = "";
  let cur = "";
  for (const ch of s + " ") {
    if (/[A-Za-z0-9]/.test(ch)) cur += ch;
    else {
      if (cur.length > best.length) best = cur;
      cur = "";
    }
  }
  return best;
}
```

---

## 32) All unique triplets summing to zero

```js
function threeSum(nums) {
  nums.sort((a, b) => a - b);
  const out = [];
  for (let i = 0; i < nums.length - 2; i++) {
    if (i > 0 && nums[i] === nums[i - 1]) continue;
    let l = i + 1, r = nums.length - 1;
    while (l < r) {
      const s = nums[i] + nums[l] + nums[r];
      if (s === 0) {
        out.push([nums[i], nums[l], nums[r]]);
        l++;
        r--;
        while (l < r && nums[l] === nums[l - 1]) l++;
        while (l < r && nums[r] === nums[r + 1]) r--;
      } else if (s < 0) l++;
      else r--;
    }
  }
  return out;
}
```

---

## 33) Compress string with run counts

```js
function compressStringRuns(s) {
  if (s.length === 0) return "";
  let out = "";
  let count = 1;
  for (let i = 1; i <= s.length; i++) {
    if (s[i] === s[i - 1]) count++;
    else {
      out += s[i - 1] + String(count);
      count = 1;
    }
  }
  return out;
}
```

---

## 34) Reverse array without reverse()

```js
function reverseArrayInPlace(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    [nums[l], nums[r]] = [nums[r], nums[l]];
    l++;
    r--;
  }
  return nums;
}
```

---

## 35) Remove chars of second string from first

```js
function removeChars(first, second) {
  const ban = new Set(second);
  let out = "";
  for (const ch of first) if (!ban.has(ch)) out += ch;
  return out;
}
```

---

## 36) Longest increasing subsequence

```js
function lengthOfLIS(nums) {
  const tails = [];
  for (const x of nums) {
    let l = 0, r = tails.length;
    while (l < r) {
      const m = (l + r) >> 1;
      if (tails[m] < x) l = m + 1;
      else r = m;
    }
    tails[l] = x;
  }
  return tails.length;
}
```

---

## 37) Check if one string is rotation of another

```js
function isRotation(a, b) {
  if (a.length !== b.length) return false;
  return (a + a).includes(b);
}
```

---

## 38) Count pairs differing by exactly k

```js
function countPairsDiffK(nums, k) {
  if (k < 0) return 0;
  const freq = new Map();
  for (const x of nums) freq.set(x, (freq.get(x) || 0) + 1);
  let ans = 0;
  if (k === 0) {
    for (const c of freq.values()) if (c > 1) ans++;
    return ans;
  }
  for (const x of freq.keys()) if (freq.has(x + k)) ans++;
  return ans;
}
```

---

## 39) Roman numeral to integer

```js
function romanToInt(s) {
  const v = new Map([
    ["I", 1], ["V", 5], ["X", 10], ["L", 50],
    ["C", 100], ["D", 500], ["M", 1000],
  ]);
  let ans = 0;
  for (let i = 0; i < s.length; i++) {
    const cur = v.get(s[i]);
    const next = i + 1 < s.length ? v.get(s[i + 1]) : 0;
    ans += cur < next ? -cur : cur;
  }
  return ans;
}
```

---

## 40) First non-repeating element in array

```js
function firstUniqueElement(nums) {
  const freq = new Map();
  for (const x of nums) freq.set(x, (freq.get(x) || 0) + 1);
  for (const x of nums) if (freq.get(x) === 1) return x;
  return null;
}
```

---

## 41) Pangram test

```js
function isPangram(sentence) {
  const seen = new Set();
  for (const ch of sentence.toLowerCase()) {
    const code = ch.charCodeAt(0);
    if (code >= 97 && code <= 122) seen.add(ch);
  }
  return seen.size === 26;
}
```

---

## 42) Merge overlapping intervals

```js
function mergeIntervals(intervals) {
  if (intervals.length === 0) return [];
  intervals.sort((a, b) => a[0] - b[0]);
  const out = [intervals[0].slice()];
  for (let i = 1; i < intervals.length; i++) {
    const [s, e] = intervals[i];
    const last = out[out.length - 1];
    if (s <= last[1]) last[1] = Math.max(last[1], e);
    else out.push([s, e]);
  }
  return out;
}
```

---

## 43) All palindromic substrings

```js
function allPalindromicSubstrings(s) {
  const out = [];
  for (let i = 0; i < s.length; i++) {
    expand(i, i);
    expand(i, i + 1);
  }
  return out;

  function expand(l, r) {
    while (l >= 0 && r < s.length && s[l] === s[r]) {
      out.push(s.slice(l, r + 1));
      l--;
      r++;
    }
  }
}
```

---

## 44) Longest consecutive sequence in array

```js
function longestConsecutive(nums) {
  const st = new Set(nums);
  let best = 0;
  for (const x of st) {
    if (!st.has(x - 1)) {
      let cur = x, len = 1;
      while (st.has(cur + 1)) {
        cur++;
        len++;
      }
      best = Math.max(best, len);
    }
  }
  return best;
}
```

---

## 45) Remove repeated sequential characters

```js
function removeSequentialDuplicates(s) {
  if (!s) return "";
  let out = s[0];
  for (let i = 1; i < s.length; i++) if (s[i] !== s[i - 1]) out += s[i];
  return out;
}
```

## 46) Find a peak element

```js
function findPeakElement(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    const m = Math.floor((l + r) / 2);
    if (nums[m] < nums[m + 1]) l = m + 1;
    else r = m;
  }
  return l;
}
```

## 47) Check subsequence

```js
function isSubsequence(s, t) {
  let i = 0, j = 0;
  while (i < s.length && j < t.length) {
    if (s[i] === t[j]) i++;
    j++;
  }
  return i === s.length;
}
```

## 48) Sort array of 0s, 1s, and 2s

```js
function sortColors(nums) {
  let low = 0, mid = 0, high = nums.length - 1;
  while (mid <= high) {
    if (nums[mid] === 0) [nums[low++], nums[mid++]] = [nums[mid], nums[low]];
    else if (nums[mid] === 1) mid++;
    else [nums[mid], nums[high--]] = [nums[high], nums[mid]];
  }
  return nums;
}
```

## 49) Convert string chars to ASCII list

```js
function stringToAsciiList(s) {
  const out = [];
  for (const ch of s) out.push(ch.charCodeAt(0));
  return out;
}
```

## 50) Count subarrays with equal 0s and 1s

```js
function countEqualZeroOneSubarrays(nums) {
  const mp = new Map([[0, 1]]);
  let sum = 0, ans = 0;
  for (const x of nums) {
    sum += x === 0 ? -1 : 1;
    ans += mp.get(sum) || 0;
    mp.set(sum, (mp.get(sum) || 0) + 1);
  }
  return ans;
}
```

## 51) Reverse only vowels

```js
function reverseVowels(s) {
  const arr = s.split("");
  const isVowel = (c) => "aeiouAEIOU".includes(c);
  let l = 0, r = arr.length - 1;
  while (l < r) {
    while (l < r && !isVowel(arr[l])) l++;
    while (l < r && !isVowel(arr[r])) r--;
    [arr[l], arr[r]] = [arr[r], arr[l]];
    l++; r--;
  }
  return arr.join("");
}
```

## 52) Smallest subarray sum at least target

```js
function minSubarrayLen(target, nums) {
  let l = 0, sum = 0, best = Infinity;
  for (let r = 0; r < nums.length; r++) {
    sum += nums[r];
    while (sum >= target) {
      best = Math.min(best, r - l + 1);
      sum -= nums[l++];
    }
  }
  return best === Infinity ? 0 : best;
}
```

## 53) Convert sentence to title case words

```js
function toTitleCase(sentence) {
  return sentence
    .toLowerCase()
    .replace(/\b[a-z]/g, (ch) => ch.toUpperCase());
}
```

## 54) Elements appearing more than n/3 times

```js
function majorityElementsNBy3(nums) {
  let c1 = null, c2 = null, k1 = 0, k2 = 0;
  for (const x of nums) {
    if (x === c1) k1++;
    else if (x === c2) k2++;
    else if (k1 === 0) { c1 = x; k1 = 1; }
    else if (k2 === 0) { c2 = x; k2 = 1; }
    else { k1--; k2--; }
  }
  k1 = 0; k2 = 0;
  for (const x of nums) {
    if (x === c1) k1++;
    else if (x === c2) k2++;
  }
  const out = [];
  if (k1 > nums.length / 3) out.push(c1);
  if (k2 > nums.length / 3) out.push(c2);
  return out;
}
```

## 55) Check if all chars are unique

```js
function allUniqueChars(s) {
  const st = new Set();
  for (const ch of s) {
    if (st.has(ch)) return false;
    st.add(ch);
  }
  return true;
}
```

## 56) Trapping rain water

```js
function trapRainWater(height) {
  let l = 0, r = height.length - 1;
  let leftMax = 0, rightMax = 0, water = 0;
  while (l < r) {
    if (height[l] < height[r]) {
      leftMax = Math.max(leftMax, height[l]);
      water += leftMax - height[l++];
    } else {
      rightMax = Math.max(rightMax, height[r]);
      water += rightMax - height[r--];
    }
  }
  return water;
}
```

## 57) Reorganize string (no equal adjacent)

```js
function reorganizeString(s) {
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  const arr = [...freq.entries()];
  let out = "";
  while (arr.length) {
    arr.sort((a, b) => b[1] - a[1]);
    let placed = false;
    for (let i = 0; i < arr.length; i++) {
      if (out.length === 0 || out[out.length - 1] !== arr[i][0]) {
        out += arr[i][0];
        arr[i][1]--;
        if (arr[i][1] === 0) arr.splice(i, 1);
        placed = true;
        break;
      }
    }
    if (!placed) return "";
  }
  return out;
}
```

## 58) kth smallest value in array

```js
function kthSmallest(nums, k) {
  const a = [...nums].sort((x, y) => x - y);
  return k >= 1 && k <= a.length ? a[k - 1] : null;
}
```

## 59) Replace first occurrence of substring

```js
function replaceFirst(str, sub, replacement) {
  const idx = str.indexOf(sub);
  if (idx === -1) return str;
  return str.slice(0, idx) + replacement + str.slice(idx + sub.length);
}
```

## 60) Check rotated sorted array

```js
function isRotatedSorted(nums) {
  let drops = 0;
  for (let i = 0; i < nums.length; i++) {
    const next = nums[(i + 1) % nums.length];
    if (nums[i] > next) drops++;
    if (drops > 1) return false;
  }
  return true;
}
```

## 61) Count substrings starting and ending same

```js
function countSameStartEndSubstrings(s) {
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  let ans = 0;
  for (const c of freq.values()) ans += (c * (c + 1)) / 2;
  return ans;
}
```

## 62) Return all duplicate numbers

```js
function findDuplicates(nums) {
  const freq = new Map();
  for (const x of nums) freq.set(x, (freq.get(x) || 0) + 1);
  const out = [];
  for (const [x, c] of freq.entries()) if (c > 1) out.push(x);
  return out;
}
```

## 63) Validate simplified email

```js
function isValidSimpleEmail(email) {
  return /^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$/.test(email);
}
```

## 64) Count inversions in array

```js
function countInversions(nums) {
  function sortCount(arr) {
    if (arr.length <= 1) return [arr, 0];
    const mid = arr.length >> 1;
    const [L, c1] = sortCount(arr.slice(0, mid));
    const [R, c2] = sortCount(arr.slice(mid));
    let i = 0, j = 0, inv = c1 + c2, merged = [];
    while (i < L.length && j < R.length) {
      if (L[i] <= R[j]) merged.push(L[i++]);
      else { merged.push(R[j++]); inv += L.length - i; }
    }
    return [merged.concat(L.slice(i), R.slice(j)), inv];
  }
  return sortCount(nums)[1];
}
```

## 65) Capitalize first letter of each sentence

```js
function capitalizeSentences(text) {
  let out = "", capNext = true;
  for (const ch of text) {
    if (capNext && /[a-zA-Z]/.test(ch)) {
      out += ch.toUpperCase();
      capNext = false;
    } else {
      out += ch;
    }
    if (ch === "." || ch === "!" || ch === "?") capNext = true;
  }
  return out;
}
```

## 66) Container with most water

```js
function maxArea(height) {
  let l = 0, r = height.length - 1, best = 0;
  while (l < r) {
    best = Math.max(best, Math.min(height[l], height[r]) * (r - l));
    if (height[l] < height[r]) l++;
    else r--;
  }
  return best;
}
```

## 67) Check if can permute to palindrome

```js
function canPermutePalindrome(s) {
  const odd = new Set();
  for (const ch of s) {
    if (odd.has(ch)) odd.delete(ch);
    else odd.add(ch);
  }
  return odd.size <= 1;
}
```

## 68) Minimum jumps to reach end

```js
function minJumps(nums) {
  if (nums.length <= 1) return 0;
  let jumps = 0, curEnd = 0, far = 0;
  for (let i = 0; i < nums.length - 1; i++) {
    far = Math.max(far, i + nums[i]);
    if (i === curEnd) {
      jumps++;
      curEnd = far;
    }
  }
  return jumps;
}
```

## 69) Remove consecutive duplicate words

```js
function removeConsecutiveDuplicateWords(sentence) {
  const words = sentence.trim().split(/\s+/);
  if (words.length === 0) return "";
  const out = [words[0]];
  for (let i = 1; i < words.length; i++) {
    if (words[i].toLowerCase() !== words[i - 1].toLowerCase()) out.push(words[i]);
  }
  return out.join(" ");
}
```

## 70) Running sums of array

```js
function runningSum(nums) {
  const out = [];
  let sum = 0;
  for (const x of nums) {
    sum += x;
    out.push(sum);
  }
  return out;
}
```

## 71) Validate password strength rules

```js
function isStrongPassword(pw) {
  if (pw.length < 8) return false;
  const hasUpper = /[A-Z]/.test(pw);
  const hasLower = /[a-z]/.test(pw);
  const hasDigit = /\d/.test(pw);
  const hasSpecial = /[^A-Za-z0-9]/.test(pw);
  return hasUpper && hasLower && hasDigit && hasSpecial;
}
```

## 72) Group anagrams from string array

```js
function groupAnagrams(words) {
  const mp = new Map();
  for (const w of words) {
    const key = w.split("").sort().join("");
    if (!mp.has(key)) mp.set(key, []);
    mp.get(key).push(w);
  }
  return [...mp.values()];
}
```

## 73) Split string into chunks of k

```js
function chunkString(s, k) {
  const out = [];
  for (let i = 0; i < s.length; i += k) out.push(s.slice(i, i + k));
  return out;
}
```

## 74) Next greater element for each value

```js
function nextGreaterElements(nums) {
  const out = new Array(nums.length).fill(-1);
  const st = [];
  for (let i = 0; i < nums.length; i++) {
    while (st.length && nums[i] > nums[st[st.length - 1]]) {
      out[st.pop()] = nums[i];
    }
    st.push(i);
  }
  return out;
}
```

## 75) Lexicographically smallest after removing k chars

```js
function removeKCharsSmallest(s, k) {
  const st = [];
  for (const ch of s) {
    while (k > 0 && st.length && st[st.length - 1] > ch) {
      st.pop();
      k--;
    }
    st.push(ch);
  }
  while (k-- > 0 && st.length) st.pop();
  return st.join("");
}
```

## 76) Rotate array left by k

```js
function rotateLeft(nums, k) {
  const n = nums.length;
  if (!n) return nums;
  const step = ((k % n) + n) % n;
  reversePart(nums, 0, step - 1);
  reversePart(nums, step, n - 1);
  reversePart(nums, 0, n - 1);
  return nums;
}

function reversePart(arr, l, r) {
  while (l < r) [arr[l], arr[r]] = [arr[r], arr[l]], l++, r--;
}
```

## 77) Phone keypad letter combinations

```js
function phoneLetterCombinations(digits) {
  if (!digits) return [];
  const map = {
    2: "abc", 3: "def", 4: "ghi", 5: "jkl",
    6: "mno", 7: "pqrs", 8: "tuv", 9: "wxyz",
  };
  const out = [];
  function dfs(i, cur) {
    if (i === digits.length) return void out.push(cur);
    for (const ch of map[digits[i]]) dfs(i + 1, cur + ch);
  }
  dfs(0, "");
  return out;
}
```

## 78) Minimum missing positive integer

```js
function firstMissingPositive(nums) {
  const st = new Set(nums);
  let x = 1;
  while (st.has(x)) x++;
  return x;
}
```

## 79) Keep only English letters

```js
function keepOnlyLetters(s) {
  return s.replace(/[^A-Za-z]/g, "");
}
```

## 80) All contiguous subarrays

```js
function allContiguousSubarrays(nums) {
  const out = [];
  for (let i = 0; i < nums.length; i++) {
    for (let j = i; j < nums.length; j++) {
      out.push(nums.slice(i, j + 1));
    }
  }
  return out;
}
```

## 81) Common words in two sentences

```js
function commonWords(sentence1, sentence2) {
  const a = new Set(sentence1.toLowerCase().split(/\s+/).filter(Boolean));
  const b = new Set(sentence2.toLowerCase().split(/\s+/).filter(Boolean));
  const out = [];
  for (const w of a) if (b.has(w)) out.push(w);
  return out;
}
```

---

## 82) Alternate positive and negative numbers

```js
function alternatePositiveNegative(nums) {
  const pos = [];
  const neg = [];
  const zero = [];
  for (const x of nums) {
    if (x > 0) pos.push(x);
    else if (x < 0) neg.push(x);
    else zero.push(x);
  }
  const out = [];
  let i = 0, j = 0;
  while (i < pos.length && j < neg.length) {
    out.push(pos[i++], neg[j++]);
  }
  while (i < pos.length) out.push(pos[i++]);
  while (j < neg.length) out.push(neg[j++]);
  return out.concat(zero);
}
```

## 83) Longest valid parentheses substring

```js
function longestValidParentheses(s) {
  const st = [-1];
  let best = 0;
  for (let k = 0; k < s.length; k++) {
    if (s[k] === "(") st.push(k);
    else {
      st.pop();
      if (st.length === 0) st.push(k);
      else best = Math.max(best, k - st[st.length - 1]);
    }
  }
  return best;
}
```

## 84) Count how many times each element appears

```js
function elementCounts(nums) {
  const freq = new Map();
  for (const x of nums) freq.set(x, (freq.get(x) || 0) + 1);
  return freq;
}
```

## 85) Regex match with dot and star

```js
function isMatchDotStar(s, p) {
  const m = s.length, n = p.length;
  const dp = Array.from({ length: m + 1 }, () => Array(n + 1).fill(false));
  dp[0][0] = true;
  for (let j = 2; j <= n; j++) {
    if (p[j - 1] === "*") dp[0][j] = dp[0][j - 2];
  }
  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      if (p[j - 1] === "*") {
        dp[i][j] = dp[i][j - 2];
        if (p[j - 2] === "." || p[j - 2] === s[i - 1]) {
          dp[i][j] = dp[i][j] || dp[i - 1][j];
        }
      } else if (p[j - 1] === "." || p[j - 1] === s[i - 1]) {
        dp[i][j] = dp[i - 1][j - 1];
      }
    }
  }
  return dp[m][n];
}
```

## 86) Intersection of two arrays

```js
function intersection(a, b) {
  const sb = new Set(b);
  const seen = new Set();
  const out = [];
  for (const x of a) {
    if (sb.has(x) && !seen.has(x)) {
      seen.add(x);
      out.push(x);
    }
  }
  return out;
}
```

## 87) Remove chars that appear more than once

```js
function removeCharsRepeated(s) {
  const freq = new Map();
  for (const ch of s) freq.set(ch, (freq.get(ch) || 0) + 1);
  let out = "";
  for (const ch of s) if (freq.get(ch) === 1) out += ch;
  return out;
}
```

## 88) Sorted array: remove duplicates length

```js
function removeDuplicatesSortedLength(nums) {
  if (nums.length === 0) return 0;
  let k = 1;
  for (let i = 1; i < nums.length; i++) {
    if (nums[i] !== nums[k - 1]) nums[k++] = nums[i];
  }
  return k;
}
```

## 89) Count digits in a string

```js
function countDigits(s) {
  let n = 0;
  for (const ch of s) if (ch >= "0" && ch <= "9") n++;
  return n;
}
```

## 90) Pair with largest product

```js
function largestProductPair(nums) {
  if (nums.length < 2) return null;
  let max1 = -Infinity, max2 = -Infinity, min1 = Infinity, min2 = Infinity;
  for (const x of nums) {
    if (x >= max1) {
      max2 = max1;
      max1 = x;
    } else if (x > max2) max2 = x;
    if (x <= min1) {
      min2 = min1;
      min1 = x;
    } else if (x < min2) min2 = x;
  }
  return min1 * min2 > max1 * max2 ? [min1, min2] : [max1, max2];
}
```

## 91) Reverse only alphabets (mirror)

```js
function reverseOnlyAlphabets(s) {
  const arr = s.split("");
  const isLetter = (ch) => /[a-zA-Z]/.test(ch);
  let l = 0, r = arr.length - 1;
  while (l < r) {
    while (l < r && !isLetter(arr[l])) l++;
    while (l < r && !isLetter(arr[r])) r--;
    if (l < r) [arr[l], arr[r]] = [arr[r], arr[l]];
    l++;
    r--;
  }
  return arr.join("");
}
```

## 92) Subarray with sum zero exists

```js
function hasSubarraySumZero(nums) {
  const seen = new Set([0]);
  let sum = 0;
  for (const x of nums) {
    sum += x;
    if (seen.has(sum)) return true;
    seen.add(sum);
  }
  return false;
}
```

## 93) Mask string (last 4 visible)

```js
function maskLastFourVisible(s) {
  if (s.length <= 4) return s;
  return "*".repeat(s.length - 4) + s.slice(-4);
}
```

## 94) Median of two sorted arrays

```js
function findMedianSortedArrays(a, b) {
  const m = a.length, n = b.length;
  const need = Math.floor((m + n + 1) / 2);
  let i = 0, j = 0;
  let last = 0, prev = 0;
  for (let k = 0; k < need; k++) {
    prev = last;
    if (i < m && (j >= n || a[i] <= b[j])) last = a[i++];
    else last = b[j++];
  }
  if ((m + n) % 2 === 1) return last;
  let next = 0;
  if (i < m && (j >= n || a[i] <= b[j])) next = a[i];
  else next = b[j];
  return (last + next) / 2;
}
```

## 95) Maximum rearrangement (lex or numeric string)

```js
function maxRearrangedString(s) {
  return s.split("").sort((a, b) => b.localeCompare(a)).join("");
}
```

## 96) Sort by frequency (high first)

```js
function sortByFrequency(nums) {
  const freq = new Map();
  for (const x of nums) freq.set(x, (freq.get(x) || 0) + 1);
  return [...nums].sort((a, b) => {
    const fa = freq.get(a), fb = freq.get(b);
    if (fb !== fa) return fb - fa;
    return a - b;
  });
}
```

## 97) Trim without built-in trim

```js
function trimManual(s) {
  let start = 0, end = s.length - 1;
  while (start <= end && s[start] === " ") start++;
  while (end >= start && s[end] === " ") end--;
  return s.slice(start, end + 1);
}
```

## 98) Longest mountain subarray

```js
function longestMountain(nums) {
  let best = 0;
  for (let i = 1; i < nums.length - 1; i++) {
    if (nums[i] > nums[i - 1] && nums[i] > nums[i + 1]) {
      let l = i, r = i;
      while (l > 0 && nums[l - 1] < nums[l]) l--;
      while (r < nums.length - 1 && nums[r] > nums[r + 1]) r++;
      best = Math.max(best, r - l + 1);
    }
  }
  return best;
}
```

## 99) Repeating substring pattern

```js
function hasRepeatingSubstringPattern(s) {
  if (s.length < 2) return false;
  return (s + s).slice(1, -1).includes(s);
}
```

## 100) Sort strings by length

```js
function sortStringsByLength(strings) {
  return [...strings].sort((a, b) => a.length - b.length || a.localeCompare(b));
}
```
